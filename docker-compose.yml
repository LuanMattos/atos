version: "2.2"
services:
  mongo:
    image: mongo
    container_name: mongodb
    environment:
      MONGO_INITDB_ROOT_USERNAME: ibugsec
      MONGO_INITDB_ROOT_PASSWORD: k2l9g3v1
    ports:
      - "27017:27017"
#  web:
#    image: httpd:latest
#    container_name: web
#    volumes:
#      - ".:/var/www/html/bunda"
#    environment:
#        WEB_DOCUMENT_ROOT: "/var/www/html/bunda"
#    ports:
#      - "80:80"
  web2:
    image: httpd:latest
    container_name: apache_web
    restart: always
    ports:
      - "80:80"
    volumes:
      - .:/usr/local/apache2/htdocs
    environment:
          WORKDIR: "."



networks:
  mongo-compose-network:
    driver: bridge